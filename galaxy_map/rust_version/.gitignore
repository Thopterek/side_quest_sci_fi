/target
/dist

# Cargo.lock is deliberately NOT ignored. This crate ships binaries, and Rust's
# guidance for applications is to commit the lock file so that a build months
# from now resolves the same dependency versions that were tested.
